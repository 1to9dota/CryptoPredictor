services:
  crypto-predictor:
    build: .
    container_name: crypto-predictor
    restart: unless-stopped
    env_file: .env
    ports:
      - "8089:8088"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/predictor.db
